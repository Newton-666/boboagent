# TICKET-GUI-F12 —— 历史重放工具链聚合渲染（owner 2026-08-14 方案）

> 施工前必读 docs/GUI-LESSONS.md + docs/GUI-DESIGN.md 规则 6。分支 feat/ticket-gui-f12（自最新 main 切出，**与 F11 独立分支**）。未 commit 等终审。

## 背景（owner 实弹截图实证）

重启/切回会话后历史重放：正文+思考折叠框都在，**工具卡全部消失**（只有带 inline_diff 的工具渲染了）——过程读起来像自言自语，看不到它调用工具做了什么。owner 原话："不是很好看。"

## 根因（Kimi 已定位）

index.html 历史渲染 `m.role === 'tool'` 分支只在 `m.inline_diff` 存在时 renderHistToolDiff，普通工具调用直接丢弃。

## 修法（owner 定调的两条）

1. **过往回合的思考+工具链收进聚合卡**：沿用 F6C 聚合卡组件（考古模式），历史里每个"思考→工具×N"链渲染为一张聚合卡（标题如"思考 + 4 个工具 · 耗时"），点击可展开考古
2. **最新一轮完整展开**：历史末尾最近一条助手消息对应的工具链保持完整渲染（工具卡逐个平铺），让用户看到"最近一次干了什么"
3. inline_diff 红绿块语义不变（在聚合卡考古内照常渲染，F3-5/F8 语义保留）
4. 新 CSS 进锚点段（/* === V2C12 已有段可复用类，新增放 === F12 历史聚合 === */），取色只用色板

## 验收

- 专项 node 实跑：构造含"思考+3 工具+正文"×3 回合的 transcript 重放 → 断言前两回合各成一张聚合卡、最后一回合工具卡平铺、diff 块仍在
- F3/F6/F6B/F6C/F6D/F8/F9/F10 全部回归不破（尤其 F8 的 diff 恢复与 F9 的活引擎重放）
- 全量零回归 + md5 闸门 + L12 汇报
